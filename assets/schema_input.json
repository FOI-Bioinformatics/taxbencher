{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/FOI-Bioinformatics/taxbencher/master/assets/schema_input.json",
    "title": "FOI-Bioinformatics/taxbencher pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces"
            },
            "classifier": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Classifier name must be provided and cannot contain spaces"
            },
            "taxpasta_file": {
                "type": "string",
                "pattern": "^\\S+\\.(tsv|txt|kreport|out|profile|mpa|kraken|centrifuge|kaiju|bracken|ganon|motus|megan|rma6)$",
                "errorMessage": "Input file must be provided and have a valid extension (.tsv/.txt for taxpasta format, or .kreport/.out/.profile/.mpa for raw profiler outputs)"
            },
            "taxonomy_db": {
                "type": "string",
                "default": "NCBI",
                "errorMessage": "Taxonomy database should be specified (default: NCBI)"
            }
        },
        "required": ["sample", "classifier", "taxpasta_file"]
    }
}
