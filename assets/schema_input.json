{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/FOI-Bioinformatics/taxbencher/master/assets/schema_input.json",
    "title": "FOI-Bioinformatics/taxbencher pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces"
            },
            "classifier": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Classifier name must be provided and cannot contain spaces"
            },
            "taxpasta_file": {
                "type": "string",
                "pattern": "^\\S+\\.(tsv|txt)$",
                "errorMessage": "Taxpasta file must be provided, cannot contain spaces, and must have extension '.tsv' or '.txt'"
            },
            "taxonomy_db": {
                "type": "string",
                "default": "NCBI",
                "errorMessage": "Taxonomy database should be specified (default: NCBI)"
            }
        },
        "required": ["sample", "classifier", "taxpasta_file"]
    }
}
